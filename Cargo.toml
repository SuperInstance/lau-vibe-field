[package]
name = "lau-vibe-field"
version = "0.1.0"
edition = "2021"
description = "CUDA/cuDNN of PLATO — the vibe field as a first-class compute primitive"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
